# Domain Knowledge - Architecture Blueprint

## 1. Stack Allocation
* **Frontend Framework:** Streamlit (Python) running custom CSS injection layers (`unsafe_allow_html=True`) to alter standard component models into specific layouts.
* **Data Warehouse:** Google Cloud BigQuery (processing StatsBomb schema event streams).
* **Orchestration & Automation:** Google Antigravity Python SDK (running asynchronous background loops to handle data scraping and BigQuery metric updates).
* **NLP Processing Hub:** Google AI Studio (Prototyping and serving the core text-distillation prompts).

## 2. Ingestion & Storage Architecture
To eliminate ongoing database interaction costs, data must be structured into flat JSON files:
* `/data/bracket/grid_state.json`: Holds Section 1 live bracket configurations and structural paths.
* `/data/standings/group_tables.json`: Stores points, goal differentials, and underlying metrics ($xG$, field tilt) for Section 2.
* `/data/matches/{match_id}/summary.json`: Ingests the NLP-purified tactical bullet narratives generated through Google AI Studio.
* `/data/matches/{match_id}/metrics.json`: Houses the pre-calculated team and player data frames generated by `fifa_metrics_bq.py`.

## 3. Component Blueprint

```
                 +---------------------------------------+
                 |         STREAMLIT FRONTEND            |
                 +---------------------------------------+
                               |          |
     +-------------------------+          +-------------------------+
     |                                                              |
     v                                                              v

+-----------------------------------+                       +-----------------------------------+
|     SECTION 1: THE BRACKET GRID   |                       |    SECTION 3: DEEP MATCH PANEL   |
+-----------------------------------+                       +-----------------------------------+
| * Custom CSS Injection            |                       | * AI Studio Distilled Summary     |
| * Font: 'Permanent Marker'        |                       | * Dixon-Coles Poisson Forecast    |
| * Tape-textured grid cells        |                       | * Your `mplsoccer` Pass Networks  |
+-----------------------------------+                       +-----------------------------------+
```
